{
  "provider": "stripe",
  "category": "payments",
  "sdkPackage": "stripe",
  "sdkVersion": "17.5.0",
  "apiVersion": "2025-06-30",
  "authType": "bearer",
  "credentials": {
    "secretKeyEnv": "STRIPE_SECRET_KEY",
    "webhookSecretEnv": "STRIPE_WEBHOOK_SECRET"
  },
  "endpoints": [
    "POST https://api.stripe.com/v1/charges",
    "POST https://api.stripe.com/v1/customers"
  ],
  "expected": {
    "sdkVersion": "17.5.0",
    "apiVersion": "2025-06-30"
  },
  "status": "current",
  "notes": "Uses the deprecated Charges API and a very old pinned SDK major. Should migrate to PaymentIntents."
}
